package com.beaver.feb122.student;

public interface StudentMapper {
	// method 리턴타입 // 추상메소드 작성
	//	insert / update / delete
	//		=> 영향을 받은 데이터 수 - int(리턴타입)
	// select
	//		결과가 하나 나온다 : resultType객체 (Student)
	//		결과가 여러개 나온다 : List<resultType객체> (List<Student>)
}
